import {ChevronLeft,ChevronRight} from "lucide-react"; import {useEffect,useState} from "react"; import {slides} from "../utils/siteData";
export default function StoryCarousel(){const [index,setIndex]=useState(0); const [paused,setPaused]=useState(false); useEffect(()=>{if(paused)return;const id=setInterval(()=>setIndex(i=>(i+1)%slides.length),5200);return()=>clearInterval(id)},[paused]); const slide=slides[index]; const move=(n)=>{setPaused(true);setIndex((index+n+slides.length)%slides.length)}; return <section className="story" id="story"><div className="story-image"><img src={slide.image} alt=""/></div><div className="story-copy"><p className="eyebrow">{slide.label}</p><h2>{slide.title}</h2><p>{slide.text}</p><div className="carousel-controls"><button onClick={()=>move(-1)} aria-label="Previous story"><ChevronLeft/></button><span>{String(index+1).padStart(2,"0")} / 05</span><button onClick={()=>move(1)} aria-label="Next story"><ChevronRight/></button></div><div className="dots">{slides.map((_,i)=><button key={i} className={i===index?"active":""} aria-label={`Show story ${i+1}`} onClick={()=>{setPaused(true);setIndex(i)}}/>)}</div></div></section>}
