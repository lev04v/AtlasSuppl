import {useEffect,useState} from "react";import {slides} from "../utils/siteData";
export default function StoryCarousel(){const [index,setIndex]=useState(0);const [paused,setPaused]=useState(false);useEffect(()=>{if(paused)return;const id=setInterval(()=>setIndex(i=>(i+1)%slides.length),5000);return()=>clearInterval(id)},[paused]);const slide=slides[index];return <section className="story" id="story" onMouseEnter={()=>setPaused(true)} onMouseLeave={()=>setPaused(false)}><div className="story-image"><img src={slide.image} alt=""/></div><div className="story-copy"><p className="eyebrow">{slide.label}</p><h2>{slide.title}</h2><p>{slide.text}</p><a className="story-scroll" href="#products">Scroll to explore <span/></a><div className="dots">{slides.map((_,i)=><button key={i} className={i===index?"active":""} aria-label={`Show story ${i+1}`} onClick={()=>{setPaused(true);setIndex(i)}}/>)}</div></div></section>}
