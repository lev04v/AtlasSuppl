# Reusable UI components

Use this folder for reusable elements such as `Navbar.jsx`, `Hero.jsx`, `ProductUniverse.jsx`, `OurPartners.jsx`, and `Footer.jsx`. The delivered refined homepage currently keeps these small page sections together in `../App.jsx` for easier installation.
