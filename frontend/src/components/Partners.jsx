import PartnerCube from "../3d/PartnerCube";
const mainLogos=Object.entries(import.meta.glob("../assets/partners/all/{abbottm,abbottnutrition,bayer,Bbraun,smithnephew,merck,healthium,cardinal,otsuka,Meril}.*",{eager:true,query:"?url",import:"default"})).sort(([a],[b])=>a.localeCompare(b)).map(([,url])=>url);
export default function Partners(){return <section className="partners section" id="partners"><div><p className="eyebrow">Our partner network</p><h2>Recognised names across the medical supply landscape.</h2><p className="muted">A true interactive 3D partner cube. Drag to explore each face; rotation pauses while you interact.</p></div><PartnerCube/><div className="partner-list main-partners" aria-label="Selected main partners">{mainLogos.map((logo,i)=><img key={i} src={logo} alt={`Authorised partner ${i+1}`}/>)}</div></section>}
