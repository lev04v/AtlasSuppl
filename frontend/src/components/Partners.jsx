import {partners} from "../utils/siteData";
export default function Partners(){return <section className="partners section" id="partners"><div><p className="eyebrow">Our partner network</p><h2>Recognised names across the medical supply landscape.</h2><p className="muted">Authorised partner logos are shown as supplied. A complete partner directory can be added as the final list is confirmed.</p></div><div className="cube-scene"><div className="logo-cube">{partners.slice(0,9).map((logo,i)=><span key={i}><img src={logo} alt="Authorised partner logo"/></span>)}</div></div><div className="partner-list" aria-label="Partner directory">{partners.map((logo,i)=><img key={i} src={logo} alt="Authorised partner logo"/>)}</div></section>}
